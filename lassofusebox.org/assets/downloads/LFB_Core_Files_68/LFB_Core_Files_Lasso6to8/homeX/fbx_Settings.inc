<?LassoScript/*<fusedoc fuse="fbx_Settings.inc" language="Lasso" specification="3.0">	<responsibilities>		I set up the enviroment settings for this circuit. If this settings file is being inherited, then you can use VARIABLE to override		a value set in a parent circuit or FBX_PARAM to accept a value set by a parent circuit	</responsibilities>		<properties>		<history author="Tami Williams" date="June 2008" email="tami@asktami.com" type="Create" />	</properties>	<io>		<in>			<structure name="fusebox">				<boolean name="isRootCircuit" comments="Is the circuit currently executing the root circuit?" />			</structure>		</in>		<out>			<string name="lid" scope="variables" />		</out>	</io></fusedoc>*//*// if $lid (the session variable for loginNameID)// if it is empty display the "you need to re-login" page//	 this prevents users from coming back to a bookmarked page that requires they login// first part of login process is to capture this variableif: (var:'lid') == '' ;redirect_url: $self + 'home.login_error' ;/if;*/var:'XFA_popupLasso' =  $self + 'popups.popwin' ;?>